# Google Sheets Configuration
# Get SPREADSHEET_ID from your Google Sheet URL:
# https://docs.google.com/spreadsheets/d/YOUR_SPREADSHEET_ID_HERE/edit
SPREADSHEET_ID=your-spreadsheet-id-here

# Sheet tab name (the name at the bottom of the spreadsheet)
# Default is "Sheet1" if not specified
SHEET_NAME=Sheet1

# Option 1: Path to credentials JSON file (for local development)
GOOGLE_SHEETS_CREDENTIALS_FILE=credentials.json

# Option 2: Full credentials JSON string (for production/deployment)
# GOOGLE_CREDENTIALS_JSON={"type":"service_account",...}

# Environment (development, test, production)
# Controls log levels: development=DEBUG, test=WARNING, production=INFO
ENVIRONMENT=development

